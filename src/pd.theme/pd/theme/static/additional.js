
$(document).on('ready', function() {
    var topnav = $('#topnav .pd-portal-globalnav');
    var offnav = $('#offCanvas .pd-portal-globalnav');

    // if (document.documentElement.clientWidth >= 1024) {
    //     offnav.remove();
    // }

    var switchNav = function switchNavF(nav) {

    });

});
